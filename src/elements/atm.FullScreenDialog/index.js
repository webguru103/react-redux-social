import React from 'react';
import Dialog from 'react-toolbox/lib/dialog';

import theme from './styles.scss';

const PPFullScreenDialog = (props) => <Dialog {...props} theme={theme} />;

export default PPFullScreenDialog;
